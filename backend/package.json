{
  "name": "off-chase-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=24.13.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.ts",
    "test:chain": "hardhat test",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test && npm run test:chain"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "@supabase/supabase-js": "^2.116.0",
    "canonicalize": "^5.1.0",
    "express": "^5.2.1",
    "viem": "^2.56.8",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.7",
    "@types/express": "^5.0.6",
    "@types/node": "^26.6.2",
    "hardhat": "^3.17.0",
    "typescript": "^7.0.2"
  }
}
